language: python
python:
  - "2.7"
  - "3.5"

# command to install dependencies
install:
  - pip install requests
# command to run tests
script:
  - python weather.py
  - python weather.py Boise
  - python weather.py Boise extraArg